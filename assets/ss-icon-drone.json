{
  "id": "ss.icon.drone",
  "name": "Shell orbiter icon",
  "description": "UI slot icon for the shell orbiter. Composes `ss.proj.drone` via `use` so the plate in the slot is the plate on screen, replacing the old hand-drawn i_drone (flat hexagon, navy core). Rim keeps the original's aqua.",
  "tags": ["ss-icon"],
  "size": [32, 32],
  "parts": [
    { "id": "tile", "shape": { "kind": "rect", "w": 32, "h": 32, "corner": 6 }, "fill": "$aqua" },
    { "id": "inner", "shape": { "kind": "rect", "w": 28, "h": 28, "corner": 5 }, "fill": "$coal.dark" },
    { "id": "glyph", "use": "ss.proj.drone", "scale": 1.3 }
  ],
  "variants": {
    "evolved": {
      "description": "Gold-rimmed evolved twin (the old i_drone_evo), derived rather than drawn again.",
      "set": { "tile.fill": "$gold" }
    }
  }
}
